function Pruning(nodes, grid) {

    let node = nodes[Math.floor(Math.random() * nodes.length)];

    console.log('Pruning played');
    console.log(nodes);

    return node;
}

export default Pruning;